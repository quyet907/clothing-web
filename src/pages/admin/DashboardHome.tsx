import { Typography } from '@material-ui/core'
import React from 'react'

export default function DashboardHome() {
    return (
        <div>
            <Typography style={{fontWeight: "bold"}} variant="h5">Dashboard</Typography>
        </div>
    )
}
